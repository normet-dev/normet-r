% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_model_train.r
\name{nm_build_model}
\alias{nm_build_model}
\title{Prepare Data and Train a Model}
\usage{
nm_build_model(
  df,
  value,
  backend = "h2o",
  feature_names = NULL,
  split_method = "random",
  fraction = 0.75,
  model_config = NULL,
  seed = 7654321,
  n_cores = NULL,
  verbose = TRUE,
  drop_time_features = FALSE
)
}
\arguments{
\item{df}{The raw input data frame.}

\item{value}{A string indicating the target column in `df`.}

\item{backend}{A string for the modeling backend. Defaults to 'h2o'.}

\item{feature_names}{A character vector of predictor columns to use. Must not be empty.}

\item{split_method}{A string for the data splitting strategy. Default is 'random'.}

\item{fraction}{A numeric value for the training fraction of the split. Default is 0.75.}

\item{model_config}{A list of backend-specific configurations to be passed to the trainer.}

\item{seed}{An integer for the random seed. Default is 7654321.}

\item{n_cores}{An integer for the number of CPU cores to use (primarily for H2O).}

\item{verbose}{A logical value to enable logging messages. Default is TRUE.}

\item{drop_time_features}{A logical value. If TRUE, helper time features
(like "date_unix", "day_julian") are dropped. Default is FALSE.}
}
\value{
A list containing two elements:
  \item{df_prep}{The prepared data frame with training/testing sets.}
  \item{model}{The trained model object.}
}
\description{
\code{nm_build_model} prepares the input data and trains a model using a selected backend.
It serves as a wrapper around the `nm_prepare_data` and `nm_train_model` functions.
}
